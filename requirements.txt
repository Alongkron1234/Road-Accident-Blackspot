# Issue 01: dependency พื้นฐานสำหรับ dev environment
# Issue ถัดไปจะทยอยเพิ่ม (tenacity ใน 04, psycopg2 ใน 05, dbt ใน 07, folium ใน 11 ฯลฯ)

python-dotenv==1.0.1
pytest==8.3.3

# Issue 02: ใช้ใน scripts/api_probe.py (Python 3.13 จาก python.org ไม่ผูก
# system cert store เหมือน curl ต้องใช้ requests ที่พก certifi มาเอง)
requests==2.32.3

# Issue 03: ใช้ทดสอบ connect เข้า Postgres จาก host (จะใช้ต่อใน extract/loader.py ของ Issue 05)
psycopg2-binary==2.9.9

# Issue 04: retry logic ให้ extractor กัน API รัฐ timeout/500
tenacity==9.0.0

# Issue 04: mock HTTP request ใน unit test ไม่ต้องยิง API จริงตอนรัน pytest
responses==0.25.3

# Issue 07: dbt ต่อ Postgres แปลง raw layer เป็น staging layer
# ต้อง pin dbt-core ควบคู่กันด้วย ไม่งั้น pip อาจ resolve เป็น dbt-core 2.x (pre-release)
# ที่ใช้ build mechanism ใหม่ ดาวน์โหลด wheel จาก GitHub แล้ว SSL cert พังบนเครื่องนี้
dbt-core==1.8.2
dbt-postgres==1.8.2
